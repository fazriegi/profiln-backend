version: "2"
sql:
  # auth sqlc
  - engine: "postgresql"
    queries: "package/auth/repository/auth-queries.sql"
    schema: "db/migrations"
    gen:
      go:
        package: "db"
        out: "db/sqlc"

  # homepage sqlc
  - engine: "postgresql"
    queries: "package/homepage/repository/homepage-queries.sql"
    schema: "db/migrations"
    gen:
      go:
        package: "db"
        out: "db/sqlc"

  # posts sqlc
  - engine: "postgresql"
    queries: "package/posts/repository/post-queries.sql"
    schema: "db/migrations"
    gen:
      go:
        package: "db"
        out: "db/sqlc"

  # profile sqlc
  - engine: "postgresql"
    queries: "package/profile/repository/profile-queries.sql"
    schema: "db/migrations"
    gen:
      go:
        package: "db"
        out: "db/sqlc"

