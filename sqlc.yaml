version: "2"
sql:
  # auth sqlc
  - engine: "postgresql"
    queries: "package/auth/repository/queries.sql"
    schema: "db/migrations"
    gen:
      go:
        package: "auth"
        out: "package/auth/repository/sqlc"

